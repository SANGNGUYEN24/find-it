<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disable background -->
    <item android:color="@color/grey" android:state_enabled="false" />

    <!-- Enabled background -->
    <item android:color="@color/blue_secondary" />
</selector>